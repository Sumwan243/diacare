import 'package:flutter/material.dart';

class MealTab extends StatelessWidget {
  const MealTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Meal Logging Tab'),
    );
  }
}
